 <!DOCTYPE html>
<html>
<head>
    <title>@ViewBag.Title</title>
    <link href="@Url.Content("/Content/Site.css")" rel="stylesheet" type="text/css" />
    <script src="@Url.Content("~/Scripts/jquery-1.5.1.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Content/KensJScript.js")" type="text/javascript" ></script>

    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <script charset="UTF-8" type="text/javascript" src="http://ecn.dev.virtualearth.net/mapcontrol/mapcontrol.ashx?v=7.0">
    </script>

    <script type="text/javascript">
        if (document.cookie["mylat"] == "null")
        {lat = 55.0; }
        else {
            lat = readCookie("mylat");
        }
        if (document.cookie["mylong"] == "null")
        {longitude = -122.8; }
        else {
            longitude = readCookie("mylong");
        }
    var map = null;
    var credentials = null;
    </script>

    


    <script type="text/javascript">
        function GetMap(zLevel, lat, longitude) {

            map = new Microsoft.Maps.Map(document.getElementById("mapDiv"),
            { credentials: "Ar0aDgj4Y-As2wCE7_iNELR306VP_gqusZtyAipfRhCrJo2_KQn4kyLminHPZbj_",
                center: new Microsoft.Maps.Location(lat, longitude),
                mapTypeId: Microsoft.Maps.MapTypeId.aerial,
                zoom: zLevel
            });

            Microsoft.Maps.Events.addHandler(map, 'click', displayEventInfo);

        }

        </script>

          <script type="text/javascript">
        function displayEventInfo(e) {
            if (e.targetType == "map") {
            var point = new Microsoft.Maps.Point(e.getX(), e.getY());
            var loc = e.target.tryPixelToLocation(point);
            if (document.getElementById("textBox")) {
                document.getElementById("textBox").value = loc.latitude + ", " + loc.longitude;
            }
            if (document.getElementById("lat")) {
                var latDiv = document.getElementById("lat").value = loc.latitude;
                var longDiv = document.getElementById("longitude").value = loc.longitude;
                }
              }
    }

     </script>
    
   
<script type="text/javascript">

    var _gaq = _gaq || [];
    _gaq.push(['_setAccount', 'UA-23348327-1']);
    _gaq.push(['_trackPageview']);

    (function () {
        var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
    })();

</script>
</head>

<body onload="GetMap(15,lat,longitude);">
    <div class="header">
        <h2>
            TreeTagger</h2>
        <h3>
            Social Tree Mapping</h3>
        <ul>
            <li><a href="/Home/index">Home</a></li>
            <li><a href="/TreeAdd/Index">List Trees</a></li>
            <li><a href="/TreeAdd/Create">Add Tree</a></li>
            <li><a href="/Home/About">About</a></li>
        </ul>
    </div>
    @RenderBody()
</body>

 
</html>